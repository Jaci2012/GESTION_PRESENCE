{% extends 'base.html.twig' %}

{% block content %}
    <h1>Listes des parcours</h1>
    <a href="{{ path('careers_add') }}" class="btn btn-primary">Ajouter un parcours</a>
    <br/><br/>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#ID</th>
                <th>Designation</th>
            </tr>
        </thead>
        <tbody>
            {% for career in careers %}
                <tr>
                    <td>{{ career.id }}</td>
                    <td>{{ career.design }}</td>
                    <td>
                     <a href="{{ path('career_edit', {'id': career.id}) }}" class="btn btn-success btn-sm">Modifier</a>
                    <a href=""
                    onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?')" class="btn btn-danger btn-sm">
                        Supprimer
                    </a>                   
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}